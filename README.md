# File Backup & Versioning System

This project automatically creates backups of a file or folder, keeps only the last 5 versions, and deletes older ones to save space.

## How to use
1. Put folder path in config.txt
2. Run ./backup.sh
3. Check backups/ folder for output
4. Check backup.log for backup history
